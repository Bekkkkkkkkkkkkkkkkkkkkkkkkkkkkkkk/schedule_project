---
  server:
    port: 8080

  spring:
    datasource:
      url: jdbc:postgresql://localhost/javaDB
      username: postgres
      password: invoker
      driver-class-name: org.postgresql.Driver
    jpa:
      hibernate:
        ddl-auto: create
      show-sql: true
